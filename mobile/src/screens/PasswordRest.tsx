import React from "react";
import { StyleSheet, Text, View } from "react-native";

export default function PasswordRest() {
	return (
		<View>
			<Text>Password Rest</Text>
		</View>
	);
}

const styles = StyleSheet.create({});
